import React from 'react';
import { Container } from 'react-bootstrap';
import Header from '../../shared/header/Header';

const Home = () => {
    return (
        <Container>
            <Header></Header>
        </Container>
    );
};

export default Home;